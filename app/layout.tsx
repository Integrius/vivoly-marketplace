import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Vivoly - AI Services Marketplace",
  description: "Marketplace platform for AI-based SaaS solutions and custom AI services",
  keywords: ["AI", "SaaS", "Marketplace", "Artificial Intelligence", "Custom Solutions"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
